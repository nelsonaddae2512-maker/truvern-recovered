const { PrismaClient } = require("@prisma/client");
const {
  buildSignedGovernanceManifest,
} = require("./.next/server/chunks/ssr/[root-of-the-server]__*.js");

console.log("Use TS route backfill instead.");
